export class Helpers {
    public GetToken = (token: string) => {

    };

    public CreateToken = (email: string, password: string) => {
      
    };

    public IsValidPassword = (password: string, email: string) => {

    };

    public CreateError = (error: string, status: number) => {
        return { error, status };
    };
};